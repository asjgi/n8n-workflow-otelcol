{
  "identifier": "observability-request",
  "title": "Add Service to Log Collection",
  "icon": "Microservice",
  "schema": {
    "properties": {
      "service_name": {
        "title": "Service Name",
        "type": "string",
        "pattern": "^[a-z0-9-]+$",
        "description": "The name of your service (must match your Kubernetes deployment name)"
      },
      "namespace": {
        "title": "Kubernetes Namespace",
        "type": "string",
        "enum": ["st", "qa", "op"],
        "default": "st",
        "description": "Kubernetes namespace where your service is deployed (st: staging, qa: quality assurance, op: operations/production)"
      },
      "cluster": {
        "title": "Cluster Environment",
        "type": "string",
        "enum": ["st", "qa", "op"],
        "description": "The target cluster environment: st (staging), qa (quality assurance), op (operations/production)"
      },
      "pipeline_type": {
        "title": "Pipeline Type",
        "type": "string",
        "enum": ["standard", "standard-metadata", "cloudlet", "apigw", "connect", "t20"],
        "default": "standard",
        "description": "The OTEL pipeline type for log processing"
      }
    },
    "required": ["service_name", "namespace", "cluster"]
  },
  "calculationProperties": {},
  "relations": {
    "service": {
      "title": "Related Service",
      "target": "service",
      "many": false,
      "required": false
    }
  }
}